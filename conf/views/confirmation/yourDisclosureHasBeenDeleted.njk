{% extends "includes/layout.njk" %}

{% from "macros/title.njk"                         import title %}

{% from "govuk/components/panel/macro.njk" import govukPanel %}


{% block pageTitle %}
  {{ title(messages("yourDisclosureHasBeenDeleted.title")) }}
{% endblock %}

{% block mainContent %}

  <div class="govuk-main-wrapper">
    <div class="govuk-grid-row">
      <div class="govuk-grid-column-two-thirds">

        {{ govukPanel({ {# ------------------ todo similar here to jest w webjars libs------------------ #}
        titleText: messages("yourDisclosureHasBeenDeleted.heading")
        }) }}

        <p class="govuk-body">
          <span class="name-overflow">{{ messages("yourDisclosureHasBeenDeleted.p1", disclosureID, arrangementID)}}</span>
        </p>

        <p class="govuk-body">
          <span class="name-overflow">{{ messages("yourDisclosureHasBeenDeleted.p2", messageRefid)}}</span>
        </p>

        <p class="govuk-body">
          <span class="name-overflow">{{ messages("yourDisclosureHasBeenDeleted.p3", emailMessage)}}</span>
        </p>

        <p class="govuk-body">{{ homePageLink | safe }}</p>
        <p class="govuk-body">{{ betaFeedbackSurvey | safe }}</p>

      </div>
    </div>
  </div>

{% endblock %}
